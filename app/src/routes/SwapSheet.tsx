// A3.4 Swap (`/session/:id/swap/:slotId`) — tier-1 slot_swaps, then full search.
// TODO(M5): tier-1 list (provenance=swap_in_list), tier-2 search (provenance=swap_off_list),
// contraindicated rows greyed with reason, non-selectable.
export default function SwapSheet() {
  return (
    <main className="mx-auto max-w-md p-4">
      <h1 className="text-lg font-medium">Swap</h1>
      <p className="mt-2 text-sm text-slate-400">Scaffold stub.</p>
    </main>
  );
}
