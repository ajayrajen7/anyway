// A3 Weigh-in (`/weigh`) — Sundays only, blind entry (value hidden on save).
// TODO(M1/M9): enforce Sunday-only + Vault lock (423 before start+84d) server-side.
export default function WeighIn() {
  return (
    <main className="mx-auto max-w-md p-4">
      <h1 className="text-lg font-medium">Weigh-in</h1>
      <p className="mt-2 text-sm text-slate-400">Scaffold stub.</p>
    </main>
  );
}
