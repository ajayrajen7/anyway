// A3.5 Add exercise (`/session/:id/add`) — search, then mandatory "Whose call?" sheet.
// Highest-value field in the app (provenance=added, added_by). Not skippable.
// TODO(M5): search picker + attribution sheet, POST /api/sessions/:id/add.
export default function AddExercise() {
  return (
    <main className="mx-auto max-w-md p-4">
      <h1 className="text-lg font-medium">Add exercise</h1>
      <p className="mt-2 text-sm text-slate-400">Scaffold stub.</p>
    </main>
  );
}
