// A3.2 Today (`/`) — session card, mobility checkbox, evening protein row.
// TODO(M3): session card + start-session; TODO(M6/M7): mobility + protein rows.
export default function Today() {
  return (
    <main className="mx-auto max-w-md p-4">
      <h1 className="text-lg font-medium">Today</h1>
      <p className="mt-2 text-sm text-slate-400">Scaffold stub — see docs/implementation-plan.md.</p>
    </main>
  );
}
