# Copy to .env.local (Vite loads it automatically; git-ignored).
# Base URL of the Go API — empty string when proxied on the same origin.
VITE_API_BASE=http://localhost:8080
# Single static bearer token (B1) — must match the server's ANYWAY_API_TOKEN.
VITE_API_TOKEN=
